<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>expressCRUD</title>

    <!-- Bootstrap CSS WAJIB di head -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <!-- BACKGROUND FULL -->
    <div class="container-fluid px-0" style="background-color:#EFF8FF;">

        <!-- ISI KONTEN -->
        <div class="container py-4">

            <form action="<%= isEdit ? '/edit/' + user.id : '/add' %>" method="POST">
                <div class="input-group mb-3">
                    <input type="text" class="form-control" name="name" placeholder="Insert your name" value="<%= user ? user.name : '' %>" required>
                </div>
                <div class="input-group mb-3">
                    <input type="number" class="form-control" name="height" placeholder="Insert your height" value="<%= user ? user.height : '' %>" required>
                </div>
                <div class="input-group mb-3">
                    <input type="number" step="0.01" class="form-control" name="weight" placeholder="Insert your Weight" value="<%= user ? user.weight : '' %>" required>
                </div>
                <div class="input-group mb-3">
                    <input type="date" class="form-control" placeholder="dd / mm / yy" name="birthdate" value="<%= user ? user.birthdate : '' %>" required>
                </div>
                <div class="input-group">
                    <select class="form-select" name="married" required>
                        <option value="">Have You Married?</option>
                        
                        <option value="1"
                          <%= user && user.married ? 'selected' : '' %>>
                          Yes
                        </option>
                      
                        <option value="0"
                          <%= user && !user.married ? 'selected' : '' %>>
                          Not Yet
                        </option>
                      </select>

                </div>

                <!-- BUTTON FULL WIDTH -->
                <button class="btn btn-success w-100 mb-3 mt-3">
                    <%= isEdit ? 'Update' : 'Save' %>
                </button>

                <a href="/" class="btn btn-primary w-100 mb-4">
                Cancel
                </a>
            </form>
        </div>

    </div>

</body>

</html>